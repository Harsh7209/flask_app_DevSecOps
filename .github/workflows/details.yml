# name of the workflow
name: my_details
on: 
  workflow_dispatch:
jobs:
  personal_detail:
    runs-on: ubuntu-latest

    steps:
     - name: giving my personal details to  run 
       run: echo "My name is  harsh"

  college_detail:
    runs-on: ubuntu-latest
    steps:
     - name: giving my college deatils
       run: echo "I am from NSIT "
  docker_info:
    runs-on: ubuntu-latest
    steps:
        - name: providing docker version
          run: docker --version
        - name: providing python version
          run: python --version
